const namespace = 'topNavMenu';

export const ADD_NEW = namespace + '/ADD_NEW';
export const REMOVE_LAST = namespace + '/REMOVE_LAST';

export const LOG_ME_IN = namespace + '/LOG_ME_IN'; // duplicate action name test (why we need namespacing)